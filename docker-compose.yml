services:
  db:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nitpickr
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # - ./database/backups/backups_backup_20260614_103755.sql:/docker-entrypoint-initdb.d/restore.sql
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5442:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: ./backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/nitpickr
      SECRET_KEY: super-secret-dev-key-change-in-production
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
